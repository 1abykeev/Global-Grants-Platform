services:
  web:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      # Persist the SQLite database between restarts
      - ./instance:/app/instance
    restart: unless-stopped